import 'package:flutter/material.dart';

class ListaQuestionarios extends StatelessWidget {
  const ListaQuestionarios({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('VIDA'),),
    );
  }
}